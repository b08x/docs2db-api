interface:
  display_name: "Docs2db Api"
  short_description: "Repo-specific patterns and workflows for docs2db-api"
  default_prompt: "Use the docs2db-api repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true